const menu=document.querySelector('.menu'),nav=document.querySelector('.site-header nav');menu.addEventListener('click',()=>{const o=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(o))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const dialog=document.querySelector('.lightbox'),dialogImg=dialog.querySelector('img'),dialogTitle=dialog.querySelector('h3'),dialogDetails=dialog.querySelector('.caption p'),dialogStatus=dialog.querySelector('.caption span'),dialogInquiry=dialog.querySelector('.inquiry');fetch('content.json').then(r=>r.json()).then(data=>{document.getElementById('about-text').textContent=data.about;document.getElementById('email-link').href=`mailto:${data.email}`;document.getElementById('instagram-link').href=data.instagram;const gallery=document.getElementById('gallery');data.works.forEach(work=>{const article=document.createElement('article');article.className='work reveal';const status=work.status?`<span class="status">${work.status}</span>`:'';article.innerHTML=`<button aria-label="${work.title} vergrößern"><img src="assets/${work.image}" alt="${work.title}" loading="lazy"></button><div class="work-meta"><div><h3>${work.title}</h3><p>${work.details}</p></div>${status}</div>`;gallery.appendChild(article);observer.observe(article);article.querySelector('button').addEventListener('click',()=>{dialogImg.src=`assets/${work.image}`;dialogImg.alt=work.title;dialogTitle.textContent=work.title;dialogDetails.textContent=work.details;dialogStatus.textContent=work.status||'';dialogInquiry.href=`mailto:${data.email}?subject=${encodeURIComponent('Anfrage zu '+work.title)}`;dialog.showModal()})})});dialog.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()});document.getElementById('year').textContent=new Date().getFullYear();