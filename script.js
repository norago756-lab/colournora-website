const menu=document.querySelector('.menu'),nav=document.querySelector('header nav');menu.onclick=()=>nav.classList.toggle('open');nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));const dialog=document.getElementById('dialog'),mainImg=dialog.querySelector('.main-img'),titleEl=dialog.querySelector('h3'),dl=dialog.querySelector('dl'),soldNote=dialog.querySelector('.sold-note'),inquiry=dialog.querySelector('.inquiry'),roomBtn=dialog.querySelector('[data-view=room]'),origBtn=dialog.querySelector('[data-view=original]');let current=null;function showView(type){if(!current)return;mainImg.src=type==='room'&&current.room_image?current.room_image:current.image;origBtn.classList.toggle('active',type==='original');roomBtn.classList.toggle('active',type==='room')}origBtn.onclick=()=>showView('original');roomBtn.onclick=()=>showView('room');fetch('content.json').then(r=>r.json()).then(data=>{document.getElementById('tagline').textContent=data.tagline;document.getElementById('about-text').textContent=data.about;document.getElementById('framing-note').textContent=data.framing_note+' '+data.shipping_note;document.getElementById('email-link').href='mailto:'+data.email;document.getElementById('email-link').textContent=data.email;document.getElementById('instagram-link').href=data.instagram;const g=document.getElementById('gallery');data.works.forEach(w=>{const a=document.createElement('article');a.className='work reveal';a.dataset.category=w.category;a.innerHTML=`<button><img src="${w.image}" alt="${w.title}" loading="lazy"></button><div class="work-meta"><div><h3>${w.title}</h3><p>${w.medium} · ${w.size}</p></div><span class="${w.status==='Sold'?'status-sold':''}">${w.status==='Sold'?'Sold':w.price}</span></div>`;g.appendChild(a);obs.observe(a);a.querySelector('button').onclick=()=>{current=w;mainImg.src=w.image;titleEl.textContent=w.title;dl.innerHTML=`<div><dt>Medium</dt><dd>${w.medium}</dd></div><div><dt>Size</dt><dd>${w.size}</dd></div><div><dt>Price</dt><dd>${w.price||'Sold'}</dd></div><div><dt>Framing</dt><dd>${w.framing}; Rahmung optional</dd></div><div><dt>Status</dt><dd>${w.status}</dd></div>`;soldNote.textContent=w.status==='Sold'?'Dieses Original hat bereits ein neues Zuhause gefunden. Ähnliche Arbeiten oder Auftragsarbeiten gerne auf Anfrage.':'';inquiry.href=`mailto:${data.email}?subject=${encodeURIComponent('Anfrage zu '+w.title)}`;roomBtn.hidden=!w.room_image;origBtn.classList.add('active');roomBtn.classList.remove('active');dialog.showModal()}});const list=document.getElementById('exhibition-list');data.exhibitions.forEach(x=>{const e=document.createElement('article');e.className='exhibition reveal';e.innerHTML=`<span>${x.date}</span><div><strong>${x.title}</strong><p>${x.description}</p></div><span>${x.location}</span>`;list.appendChild(e);obs.observe(e)});document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.work').forEach(w=>w.classList.toggle('hidden',b.dataset.filter!=='all'&&w.dataset.category!==b.dataset.filter))})});dialog.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()});document.getElementById('year').textContent=new Date().getFullYear();
